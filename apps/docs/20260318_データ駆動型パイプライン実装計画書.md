# データ駆動型パイプライン実装計画書

## 0. 前提・禁止事項（全フェーズ共通）

- **各フェーズ完了後に必ず `npm run precommit` を実行し、全項目がパスしてからコミットすること**
- **リファクタリングと機能追加を同一コミットに混在させないこと**
- **依頼された変更に直接関係しない差分（コメント整理・命名変更・フォーマット以外の整形）を持ち込まないこと**
- `generated/index.html` は直接編集しない。必ず `src/` を更新して `npm run build` で再生成すること
- `strict: true` の TypeScript 設定のため `any` 型は使用禁止。`unknown` またはジェネリクスで代替すること
- 既存のエクスポート関数は削除・シグネチャ変更しないこと（テストとの互換性維持）

---

## Phase 1：内部コードをデータ駆動型アーキテクチャに置き換える

**目標**：ユーザーから見た動作（完全ランダム・排他ランダムのボタン挙動）は一切変えずに、内部実装のみをデータ駆動型に置き換える。

---

### 1-1. `apps/random-picker/src/script.ts` の変更

#### 追加する型定義（ファイル先頭のexport型定義群に追記）

```typescript
/** パイプライン実行時に渡す実行コンテキスト */
export type PipelineContext = {
  /** 排他ランダム等で参照する前回の出力テキスト（改行区切り） */
  previousOutput: string;
};

/** プロセッサが受け取るパラメータの型 */
export type ProcessorParams = Record<string, number | string>;

/** パラメータスキーマの1フィールドの定義 */
export type ParamFieldSchema = {
  type: "number" | "string";
  label: string;
  default: number | string;
};

/** プロセッサの定義（メタデータ＋実行ロジック） */
export type ProcessorDef = {
  id: string;
  name: string;
  description: string;
  paramsSchema?: Record<string, ParamFieldSchema>;
  execute: (
    items: string[],
    params: ProcessorParams,
    context: PipelineContext,
  ) => string[];
};

/** ユーザーが組み立てたパイプラインの1ステップ */
export type PipelineStep = {
  id: string;
  params?: ProcessorParams;
};
```

#### 追加する定数・関数（既存の純粋ロジック関数群の直後に追記）

**`PROCESSOR_REGISTRY` の定義**（アルファベット順でキーを並べること）

```typescript
export const PROCESSOR_REGISTRY: Record<string, ProcessorDef> = {
  excludePrevious: {
    id: "excludePrevious",
    name: "前回出力を除外",
    description: "前回の出力結果に含まれる項目を除外します",
    execute: (items, _params, context) =>
      removeExcludedItems(items, splitByNewline(context.previousOutput)),
  },
  filterEmpty: {
    id: "filterEmpty",
    name: "空行除外",
    description: "空の行を除外します",
    execute: (items) => filterEmptyStrings(items),
  },
  pickRandom: {
    id: "pickRandom",
    name: "ランダム抽出",
    description: "指定した件数だけランダムに選びます",
    paramsSchema: {
      count: { type: "number", label: "抽出件数", default: 1 },
    },
    execute: (items, params) =>
      pickRandomItems(items, params["count"] as number),
  },
  trim: {
    id: "trim",
    name: "空白削除",
    description: "各行の前後の空白を削除します",
    execute: (items) => trimStrings(items),
  },
};
```

**`resolveParams` 関数**（paramsSchema のデフォルト値をマージするユーティリティ）

```typescript
/**
 * paramsSchema のデフォルト値を基準に、ステップの params をマージして返す
 */
export function resolveParams(
  def: ProcessorDef,
  stepParams?: ProcessorParams,
): ProcessorParams {
  const defaults: ProcessorParams = {};
  if (def.paramsSchema) {
    for (const [key, field] of Object.entries(def.paramsSchema)) {
      defaults[key] = field.default;
    }
  }
  return { ...defaults, ...stepParams };
}
```

**`executePipeline` 関数**

```typescript
/**
 * パイプラインステップを順番に実行し、出力テキストを返す
 */
export function executePipeline(
  inputText: string,
  steps: PipelineStep[],
  context: PipelineContext,
): string {
  let currentItems = splitByNewline(inputText);

  for (const step of steps) {
    const def = PROCESSOR_REGISTRY[step.id];
    if (!def) continue;
    const params = resolveParams(def, step.params);
    currentItems = def.execute(currentItems, params, context);
  }

  return joinByNewline(currentItems);
}
```

#### `initApp` の変更

`fullRandomBtn.onclick` と `exclusiveRandomBtn.onclick` の内部を `executePipeline` を使うように書き換える。

**`fullRandomBtn.onclick`（変更後）**

```typescript
ui.fullRandomBtn.onclick = () => {
  const inputText = ui.input.value;
  const context: PipelineContext = { previousOutput: "" };
  const outputText = executePipeline(
    inputText,
    [
      { id: "trim" },
      { id: "filterEmpty" },
      { id: "pickRandom", params: { count: 1 } },
    ],
    context,
  );
  renderOutput(ui.output, outputText);
};
```

**`exclusiveRandomBtn.onclick`（変更後）**

```typescript
ui.exclusiveRandomBtn.onclick = () => {
  const inputText = ui.input.value;
  const context: PipelineContext = {
    previousOutput: ui.output.value ?? "",
  };
  const outputText = executePipeline(
    inputText,
    [
      { id: "trim" },
      { id: "filterEmpty" },
      { id: "excludePrevious" },
      { id: "pickRandom", params: { count: 1 } },
    ],
    context,
  );
  renderOutput(ui.output, outputText);
};
```

**`inputOpenBtn.onclick` と `outputOpenBtn.onclick` は変更しない**（`applyStringArrayProcessors` を使う既存実装のまま）。

#### 既存コードへの影響

- `applyStringArrayProcessors`、`createPickRandomItemsProcessor`、`createRemoveExcludedItemsProcessor` は**削除しない・シグネチャ変更しない**（テスト互換性維持）
- 既存の純粋ロジック関数（`trimStrings`、`filterEmptyStrings` 等）は**そのまま残す**

---

### 1-2. テストの変更

#### `apps/random-picker/tests/unit.test.ts` への追記

以下を**アルファベット順（`e` → `p` → `r` の位置）**に追記する。

**`executePipeline` のテスト**

```
describe("executePipeline", () => {
```

パターン整理：

- `steps` が空のとき → 入力をそのまま改行結合して返す
- 存在しない id の step → スキップして続行
- `trim` → `filterEmpty` → `pickRandom` の順に実行される（完全ランダム相当）
- `excludePrevious` が context.previousOutput を参照して除外する
- `pickRandom` の params.count が解決されること（デフォルト値: 1）

テスト名は AGENTS.md の命名規約に従い、`describe("executePipeline")` 内に記述する。

具体的に書くべきテストケース（全件）：

```
パターン整理:
○ 01 steps空／入力あり
○ 02 存在しないid／入力あり
○ 03 trimステップ単独
○ 04 filterEmptyステップ単独
○ 05 trim→filterEmpty→pickRandom（count=1）
○ 06 trim→filterEmpty→excludePrevious→pickRandom（previousOutput使用）
○ 07 resolveParamsによるデフォルト値適用（paramsなし→count=1）
```

**`resolveParams` のテスト**

```
describe("resolveParams", () => {
```

ケース：

- `paramsSchema` が undefined のとき → 空オブジェクトを返す
- `stepParams` が undefined のとき → デフォルト値のみ返す
- `stepParams` が指定されているとき → デフォルト値をオーバーライドする

**`PROCESSOR_REGISTRY` のテスト**

```
describe("PROCESSOR_REGISTRY", () => {
```

ケース：

- `trim`、`filterEmpty`、`pickRandom`、`excludePrevious` の4キーが存在する
- 各 `execute` が `string[]` を返す（正常系のみで可）

#### `apps/random-picker/tests/integration.test.ts` の確認

`initApp` のテストで「完全ランダムボタンを押すと出力欄に1件表示される」「排他ランダムボタンを押すと前回出力が除外される」の挙動が変わっていないことを確認する。テストが通れば変更不要。

---

### 1-3. フェーズ1の完了条件

- `npm run precommit` が全項目パスすること
- 追加したすべてのエクスポート（`executePipeline`、`resolveParams`、`PROCESSOR_REGISTRY`、各型）に対するユニットテストが存在すること
- ブラウザで `generated/index.html` を開き、完全ランダム・排他ランダムが従来通り動作すること
- コミットメッセージ例：`内部実装をデータ駆動型パイプラインに置き換え（UI変更なし）`

---

## Phase 2：パイプラインビルダーUIの追加

**目標**：ユーザーが処理の種類を選択・追加・削除・並び替えでき、実行できるUIを追加する。完全ランダム・排他ランダムのボタンは**この時点ではまだ残す**。

---

### 2-1. アプリ状態（AppState）の設計

`script.ts` に以下の型と状態を追加する。

```typescript
export type AppState = {
  pipeline: PipelineStep[];
};
```

`initApp` の外で `let appState: AppState` を持つか、`initApp` の引数で受け取れるように設計する。初期値はパイプラインが空の状態とする。

---

### 2-2. `apps/random-picker/src/style.css` の変更

パイプラインビルダーUI用のスタイルを追加する。既存のスタイルは一切変更しない。

追加が必要な要素（クラス名は以下を使うこと）：

| クラス名                 | 役割                                                              |
| ------------------------ | ----------------------------------------------------------------- |
| `.pipeline-section`      | パイプラインビルダー全体のセクション                              |
| `.pipeline-label`        | 「パイプライン」のラベルテキスト                                  |
| `.pipeline-step-list`    | 追加されたステップの一覧（縦並び）                                |
| `.pipeline-step-item`    | 1ステップ分の行（ドラッグハンドル＋名前＋params入力＋削除ボタン） |
| `.pipeline-step-name`    | ステップ名のテキスト                                              |
| `.pipeline-step-params`  | paramsスキーマから生成される入力欄のコンテナ                      |
| `.pipeline-add-controls` | プロセッサ選択ドロップダウン＋追加ボタンの行                      |
| `.pipeline-run-btn`      | 「実行」ボタン                                                    |
| `.pipeline-empty-msg`    | ステップが0件のときの空状態メッセージ                             |

---

### 2-3. `apps/random-picker/src/index.html` の変更

`button-group`（完全ランダム・排他ランダムのボタン行）の**直前**に以下を挿入する：

```html
<div class="pipeline-section">
  <p class="pipeline-label">パイプライン</p>
  <div id="pipelineStepList" class="pipeline-step-list">
    <!-- JSで動的に生成 -->
  </div>
  <div class="pipeline-add-controls">
    <select id="processorSelect">
      <!-- JSで PROCESSOR_REGISTRY からオプションを自動生成 -->
    </select>
    <button id="addStepBtn" type="button">追加</button>
  </div>
  <button id="pipelineRunBtn" class="pipeline-run-btn" type="button">
    実行
  </button>
</div>
```

既存の `button-group`（`fullRandomBtn`・`exclusiveRandomBtn`）の HTML は**変更しない**。

---

### 2-4. `apps/random-picker/src/script.ts` の変更

#### `UI` 型への追加

```typescript
type UI = {
  // 既存フィールドはそのまま残す
  input: HTMLTextAreaElement;
  inputCopyBtn: HTMLButtonElement;
  inputOpenBtn: HTMLButtonElement;
  fullRandomBtn: HTMLButtonElement;
  exclusiveRandomBtn: HTMLButtonElement;
  output: HTMLTextAreaElement;
  outputCopyBtn: HTMLButtonElement;
  outputOpenBtn: HTMLButtonElement;
  // 以下を追加
  pipelineStepList: HTMLElement;
  processorSelect: HTMLSelectElement;
  addStepBtn: HTMLButtonElement;
  pipelineRunBtn: HTMLButtonElement;
};
```

#### `createUi` の変更

新しいUI要素（`pipelineStepList`、`processorSelect`、`addStepBtn`、`pipelineRunBtn`）を `getElementByIdOrThrow` で取得してオブジェクトに追加する。

#### 追加する描画・状態管理関数

以下の関数を新たにエクスポートする（アルファベット順に配置）：

**`buildProcessorSelectOptions`**

```typescript
/**
 * PROCESSOR_REGISTRY からセレクトボックスのオプションを構築して設定する
 */
export function buildProcessorSelectOptions(
  selectEl: HTMLSelectElement,
  registry: Record<string, ProcessorDef>,
): void;
```

**`renderPipelineStepList`**

```typescript
/**
 * appState.pipeline を読んでステップ一覧のDOMを再描画する
 * - ステップが0件のとき .pipeline-empty-msg を表示する
 * - 各ステップに削除ボタン（data-index属性付き）を設ける
 * - paramsSchema が存在するステップには入力欄を生成する
 * - ドラッグ＆ドロップ用の draggable="true" と data-index を各アイテムに付与する
 */
export function renderPipelineStepList(
  listEl: HTMLElement,
  steps: PipelineStep[],
  registry: Record<string, ProcessorDef>,
): void;
```

**`addPipelineStep`**

```typescript
/**
 * appState.pipeline に1ステップを末尾追加して返す（元の配列は変更しない）
 */
export function addPipelineStep(
  steps: PipelineStep[],
  newStep: PipelineStep,
): PipelineStep[];
```

**`removePipelineStep`**

```typescript
/**
 * appState.pipeline の指定インデックスを削除して返す（元の配列は変更しない）
 */
export function removePipelineStep(
  steps: PipelineStep[],
  index: number,
): PipelineStep[];
```

**`movePipelineStep`**

```typescript
/**
 * appState.pipeline の fromIndex → toIndex へ要素を移動して返す（元の配列は変更しない）
 */
export function movePipelineStep(
  steps: PipelineStep[],
  fromIndex: number,
  toIndex: number,
): PipelineStep[];
```

**`updatePipelineStepParam`**

```typescript
/**
 * 指定インデックスのステップの params を更新して返す（元の配列は変更しない）
 */
export function updatePipelineStepParam(
  steps: PipelineStep[],
  index: number,
  key: string,
  value: number | string,
): PipelineStep[];
```

#### `initApp` の変更

`appState` のスコープと、パイプラインUIへのイベント結線を追加する。

```
initApp の責務（追加分）:
1. buildProcessorSelectOptions で processorSelect を初期化する
2. renderPipelineStepList で初期描画（空リスト）を行う
3. addStepBtn.onclick: processorSelect の値から PipelineStep を生成 → addPipelineStep → 再描画
4. pipelineStepList のクリックイベント（イベント委譲）:
   - 削除ボタン（.pipeline-delete-btn）クリック → removePipelineStep → 再描画
5. pipelineStepList の入力イベント（イベント委譲）:
   - params 入力欄の change/input → updatePipelineStepParam → appState更新（再描画しない）
6. D&D: pipelineStepList の dragstart / dragover / drop → movePipelineStep → 再描画
7. pipelineRunBtn.onclick:
   - context = { previousOutput: ui.output.value ?? "" }
   - executePipeline(ui.input.value, appState.pipeline, context) で実行
   - renderOutput で出力
```

---

### 2-5. テストの変更

#### `unit.test.ts` への追記（アルファベット順に追加）

追加する `describe` ブロック：

- `addPipelineStep`：ステップが追加されること、元配列が変更されないこと
- `buildProcessorSelectOptions`：PROCESSOR_REGISTRY の全キーがオプションとして設定されること
- `movePipelineStep`：fromIndex→toIndex の移動が正しいこと、元配列が変更されないこと
- `removePipelineStep`：指定インデックスが削除されること、元配列が変更されないこと
- `renderPipelineStepList`：steps空のとき `.pipeline-empty-msg` が表示されること、steps1件のとき `.pipeline-step-item` が1件描画されること、paramsSchemaあり処理のとき入力欄が生成されること
- `updatePipelineStepParam`：指定インデックスの params が更新されること、元配列が変更されないこと

#### `integration.test.ts` への追記

- パイプラインに `trim → filterEmpty → pickRandom(count=1)` を設定して「実行」ボタンを押すと出力欄に1件表示されること
- パイプラインが空の状態で「実行」ボタンを押すと出力欄が空になること（入力がそのまま改行結合される）

#### E2Eテスト（`e2e.spec.ts`）への追記

- プロセッサ選択ドロップダウンが存在し、選択肢が4件あること
- 「追加」ボタンでステップが追加されること
- 削除ボタンでステップが削除されること
- パイプラインを設定して「実行」ボタンを押すと出力が変わること

---

### 2-6. フェーズ2の完了条件

- `npm run precommit` が全項目パスすること
- 既存の完全ランダム・排他ランダムボタンが従来通り動作すること
- 新しいパイプラインUIで任意の処理を選択・追加・削除・並び替えして実行できること
- `pipelineRunBtn` で実行した結果が `output` に反映されること
- コミットメッセージ例：`パイプラインビルダーUIを追加（旧ボタン残留）`

---

## Phase 3：旧ボタン（完全ランダム・排他ランダム）の削除

**目標**：フェーズ2のパイプラインUIが完全に動作することを確認した後、旧ボタンを削除する。

---

### 3-1. 削除対象

#### `apps/random-picker/src/index.html`

削除するHTML要素：

```html
<div class="button-group">
  <button id="fullRandomBtn" type="button">完全ランダム</button>
  <button id="exclusiveRandomBtn" type="button">排他ランダム</button>
</div>
```

#### `apps/random-picker/src/script.ts`

**`UI` 型から削除**：

- `fullRandomBtn: HTMLButtonElement`
- `exclusiveRandomBtn: HTMLButtonElement`

**`createUi` から削除**：

- `fullRandomBtn` の `getElementByIdOrThrow` 行
- `exclusiveRandomBtn` の `getElementByIdOrThrow` 行

**`initApp` から削除**：

- `ui.fullRandomBtn.onclick = ...` ブロック全体
- `ui.exclusiveRandomBtn.onclick = ...` ブロック全体

#### `apps/random-picker/src/style.css`

`.button-group` スタイルを削除する（このクラスが他の要素に使われていないことを確認してから削除）。

---

### 3-2. テストの変更

#### `unit.test.ts`

- `initApp` に関するテストから `fullRandomBtn`・`exclusiveRandomBtn` に関するケースをすべて削除する

#### `integration.test.ts`

- `fullRandomBtn`・`exclusiveRandomBtn` を操作するテストケースをすべて削除する

#### `e2e.spec.ts`

- `fullRandomBtn`・`exclusiveRandomBtn` に関するテストケースをすべて削除する

---

### 3-3. フェーズ3の完了条件

- `npm run precommit` が全項目パスすること
- `#fullRandomBtn`・`#exclusiveRandomBtn` という id を参照するコード・テストが残っていないこと（`grep` で確認）
- ブラウザで `generated/index.html` を開き、旧ボタンが表示されていないこと
- パイプラインUIで全機能が引き続き動作すること
- コミットメッセージ例：`完全ランダム・排他ランダムボタンを削除`

---

## 各フェーズのチェックリスト（コーディングエージェント用）

### Phase 1 チェックリスト

- [ ] `PipelineContext`、`ProcessorParams`、`ParamFieldSchema`、`ProcessorDef`、`PipelineStep` 型を追加した
- [ ] `PROCESSOR_REGISTRY` を追加した（キー：`excludePrevious`、`filterEmpty`、`pickRandom`、`trim` の4件）
- [ ] `resolveParams` 関数を追加した
- [ ] `executePipeline` 関数を追加した
- [ ] `initApp` の `fullRandomBtn.onclick` を `executePipeline` 使用に書き換えた
- [ ] `initApp` の `exclusiveRandomBtn.onclick` を `executePipeline` 使用に書き換えた
- [ ] 既存エクスポート（`applyStringArrayProcessors`、`createPickRandomItemsProcessor`、`createRemoveExcludedItemsProcessor`）を削除していない
- [ ] `executePipeline`、`resolveParams`、`PROCESSOR_REGISTRY` のユニットテストを追加した
- [ ] `npm run precommit` がパスした

### Phase 2 チェックリスト

- [ ] `AppState` 型を追加した
- [ ] `UI` 型に新要素（`pipelineStepList`、`processorSelect`、`addStepBtn`、`pipelineRunBtn`）を追加した
- [ ] `createUi` で新UI要素を取得するよう更新した
- [ ] `buildProcessorSelectOptions`、`renderPipelineStepList`、`addPipelineStep`、`removePipelineStep`、`movePipelineStep`、`updatePipelineStepParam` を実装・エクスポートした
- [ ] `initApp` にパイプラインUIのイベント結線を追加した
- [ ] `src/index.html` にパイプラインビルダーUIを追加した
- [ ] `src/style.css` にパイプライン用スタイルを追加した
- [ ] 旧ボタン（`fullRandomBtn`・`exclusiveRandomBtn`）の HTML・ロジックは**残っている**
- [ ] 新関数すべてのユニットテストを追加した
- [ ] `integration.test.ts`・`e2e.spec.ts` にパイプラインUI操作テストを追加した
- [ ] `npm run precommit` がパスした

### Phase 3 チェックリスト

- [ ] `src/index.html` から `button-group` div を削除した
- [ ] `UI` 型から `fullRandomBtn`・`exclusiveRandomBtn` を削除した
- [ ] `createUi` から両要素の取得コードを削除した
- [ ] `initApp` から両ボタンの onclick ハンドラを削除した
- [ ] `src/style.css` から `.button-group` スタイルを削除した
- [ ] テストから両ボタン関連のすべてのケースを削除した
- [ ] `grep -r "fullRandomBtn\|exclusiveRandomBtn" apps/` の結果が0件であることを確認した
- [ ] `npm run precommit` がパスした
